@using Library_Management_System.Models.Entity
@model List<Movement>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>Ödünç Kitap Lİstesi</h2>
<table class="table table-striped">
    <tr style="background-color:rgb(252,178,22)">
        <th>İşlem Id</th>
        <th>Kitap Adı</th>
        <th> Üye</th>
        <th>Personel</th>
        <th>Alış Tarihi</th>
        <th>İade Tarihi</th>

        <th>Sil</th>

    </tr>
    @foreach (var k in Model)
    {
        <tr>
            <td>@k.Id</td>
            <td>@k.Book.Name</td>
            <td>@k.Members.Name &nbsp; @k.Members.Surname</td>
            <td>@k.Employee_Id</td>
            <td>@Convert.ToDateTime(@k.StartDate).ToString("dd/MM/yyyy")</td>
            <td>@Convert.ToDateTime(@k.FinishDate).ToString("dd/MM/yyyy")</td>

            <td><a href="/Entruts/EntrustReturn/@k.Id" class="btn btn-default" style="background-color:#ff0000; ">Kitabı İade Et</a></td>
        </tr>
    }
</table>

